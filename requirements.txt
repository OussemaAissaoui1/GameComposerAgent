# Game Maker Agent v1 — Dependencies
# Python 3.11+

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# PDF extraction
PyMuPDF>=1.24.0

# LLM client (OpenAI-compatible, works with Groq)
openai>=1.50.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Logging (stdlib sufficient, no extra deps needed)
